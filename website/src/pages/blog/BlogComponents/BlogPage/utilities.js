import React from 'react'

export function renderContents(contents){
    // This will render each of the contents passed into the function
    // Assume that contents is an array of react components, we want to plot these atop eachother
    return null
}